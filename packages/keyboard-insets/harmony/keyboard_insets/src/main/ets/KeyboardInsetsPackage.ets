import { RNOHPackage } from '@rnoh/react-native-openharmony';

/**
 * The component is implemented by the C-API package. This ArkTS package is
 * intentionally only the HAR/autolinking entry point for that package.
 */
export class KeyboardInsetsPackage extends RNOHPackage {}
